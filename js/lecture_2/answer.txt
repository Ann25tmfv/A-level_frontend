1) Будет не найдена, потому что не было присвоено значение для var a;
2) 2, т.к. будет доступна в скоупе с функцией
3) Обьявлена позже, поэтому будет ошибка;
4)3, потому что задана в блоке if (true) { let a=3;
5)4, потому что задана в блоке if (true) b=4;
6) 5, т.к. будет доступна в скоупе с функцией
7)Будет не найдена, потому что не было присвоено значение для var a;
8)Не задана в даном блоке, поэтому будет не найдена;
9)5, задана в блоке fuction makeSomeCoolStaff() { const c=5;
10)Будет не найдена, потому что не было присвоено значение для var a;
11)4, т.к. переменная let b=2, получила новое значение после вьіполнения функции
12)Ошибка ( const c=5 находится в блоке, поэтому ее не видит);
