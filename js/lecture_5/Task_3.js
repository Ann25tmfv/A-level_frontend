/**
Task 3.
 */

var searchingSubstring = "a", initialString ="Hello world";
function searchSubstringAmount (initialString, searchingSubstring) {
    for (var searchSubstringAmount = -1, index=0; index != -1; searchSubstringAmount++, index = initialString.indexOf (searchingSubstring, index + 1));

        return searchSubstringAmount;
}
    console.log(searchSubstringAmount (initialString, searchingSubstring));

